#include <lista.h>

//Retorna una nueva lista vacia.
lista_t lista_crear() {
	return lista_t = {0,null};
}

int lista_insertar(lista_t lista, unsigned int pos, int elem) {
	if (pos==lista.cantidad_elementos) {
		
	}
}